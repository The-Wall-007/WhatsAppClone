1. expo install react-native-safe-area-context

2. npm install --> Install Latest version
   expo install --> Install Compatible version

3. https://fonts.google.com/ --> For Fonts

4. expo install expo-splash-screen

5. expo install expo/vector-icons

6. <KeyboardAvoidingView
   style={styles.screen}
   keyboardVerticalOffset={100}
   behavior={Platform.OS === "ios" ? "padding" : undefined}
   >

7.  <ScrollView showsVerticalScrollIndicator={false}>
          <KeyboardAvoidingView
            style={styles.keyboardAvoidingView}
            behavior={Platform.OS === "ios" ? "height" : undefined}
            keyboardVerticalOffset={100}
          >
